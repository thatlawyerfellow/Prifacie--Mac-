# PriFacie Source

This folder contains the PriFacie source code and build files.

For the full packaged project guide, feature overview, and macOS build/run instructions that use both `Source/` and `Builds/`, see:

- `../README.md`
